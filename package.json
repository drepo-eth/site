{
	"name": "drepo-website",
	"version": "1.0.0",
	"description": "Decentralized Repository Website",
	"main": "index.js",
	"scripts": {
		"build": "hugo",
		"build-draft": "hugo --buildDrafts",
		"create": "hugo new",
		"server": "hugo server --buildDrafts --buildExpired",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "dRepo Team",
	"license": "MIT",
	"hugo-bin": {
		"buildTags": "extended"
	},
	"devDependencies": {
		"autoprefixer": "^10.4.14",
		"hugo-bin": "^0.100.0",
		"postcss-cli": "^10.1.0"
	},
	"dependencies": {
		"@fontsource/ibm-plex-serif": "^4.5.12",
		"@fontsource/overpass-mono": "^4.5.11",
		"bootstrap-icons": "^1.10.3",
		"normalize.css": "^8.0.1"
	}
}
